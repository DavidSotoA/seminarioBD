package com.recommendationSys

object Constants {
  // número de and functions usadas en el lsh.
  val AND_FUNCTIONS = 10

  // nombres de las columnas que tiene el dataframe de rankings.
  val COL_USER_ID = "userId"
  val COL_MOVIE_ID = "movieId"
  val COL_RATING = "rating"
  val COL_TIMESTAMP = "timestamp"
  val COL_FEATURES = "features"
  val COL_SIGNATURE = "signature"
}
